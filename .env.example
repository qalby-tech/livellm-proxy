
# Server Configuration
HOST=0.0.0.0
PORT=8000

# Logging & Monitoring
LOGFIRE_WRITE_TOKEN=
OTEL_EXPORTER_OTLP_ENDPOINT=

# Redis Configuration (Optional)
# If provided, provider settings will be stored in Redis with encryption
# Example: redis://localhost:6379/0 or redis://user:password@host:port/db
REDIS_URL=

# Redis Encryption Salt (Required if REDIS_URL is set)
# This salt is used to derive the encryption key for securing data in Redis
# Generate a strong random string, e.g., using: python -c "import secrets; print(secrets.token_urlsafe(32))"
REDIS_ENCRYPTION_SALT=